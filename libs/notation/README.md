# notation

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build notation` to build the library.

## Running unit tests

Run `nx test notation` to execute the unit tests via [Jest](https://jestjs.io).
